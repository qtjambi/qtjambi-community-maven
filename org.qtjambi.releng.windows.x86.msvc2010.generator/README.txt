
Testcase run: java -jar *-jxe.jar --help
	Check it runs, returns usage
	Run it on something basic, check the output against a control set, and success exit status.

Check to see why zlib1.dll is needed in generator.exe.